title: Visit DC
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
theme: minima

# Defines the pages to include in the header, and in what order.
# This is what the Minima theme.
header_pages:
  - index.markdown
  - categories.markdown
  - planner.markdown

exclude:
  - README.md